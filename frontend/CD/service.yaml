apiVersion: v1
kind: Service
metadata:
  name: task-manager-frontend-svc
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: task-manager-frontend-app
  ports:
    - port: 5173
      targetPort: 5173
      protocol: TCP